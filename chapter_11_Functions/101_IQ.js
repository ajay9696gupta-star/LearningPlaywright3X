const score = 100;
// Score = score + 10; // This will throw an error because score is a constant and cannot be reassigned

const pi   = 3.14;
const prod_api_url = "https://api.example.com/v1/products";

const qa_api_url = "https://qa-api.example.com/v1/products";

let abc = "anil";
console.log(abc); // This will log 'anil' because the variable is accessible within the block scope 
